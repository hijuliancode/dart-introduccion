void main() {
  bool activado = true;
  
// activado = !activado;
  
  if ( !activado ) {
    print('El motor esta funcionando');
  } else {
    print('El motor esta apagado');
  }
  
  print(activado);
}